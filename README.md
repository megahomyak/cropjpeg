Vibecoded tool for cropping images from clipboard and saving them as JPEG:

* Run `cropjpeg.py` with an image in your clipboard
* Crop the image by dragging the handles
* Press enter to save the cropped image
